{% extends 'base.html.twig' %}

{% block title %}Catégorie - {{ category.name }}{% endblock %}

{% block body %}
    <main class="container">
        <div class="row g-5">

            <div class="col">

                <h3 class="border-bottom pb-4 mb-4">{{ category.name }}</h3>
                <div id="article-list">
                    {% include 'article/list.html.twig' with { articles: category.articles, leftCol: 4, rigthCol: 8 } %}
                </div>
            </div>

        </div>
    </main>
{% endblock %}
